# 過去の進路実績ダッシュボード 開発プラン (Career Path Dashboard Development Plan)

このドキュメントは、過去の進路実績を可視化・分析するダッシュボードの開発プランをまとめたものです。ClaudeなどのAIアシスタントにコンテキストを共有するために使用します。

## 1. 現状の進捗 (Current Status)

現在、フロントエンドのベースとなるモックアップと基本的な機能が実装されています。

- **UI/UXデザイン**: モダンなグラスモーフィズムデザインのベースを作成（`index.html`, `style.css`）
- **基本機能**:
  - ローカルのCSVファイルを読み込む機能（`main.js`）
  - 読み込んだCSVデータのテーブル動的生成
  - テキスト入力による簡易的な絞り込み（全文検索）機能
- **ディレクトリ構成**:
  - `index.html` (メイン画面)
  - `style.css` (スタイリング)
  - `main.js` (JavaScriptロジック)
  - `input/` (CSVファイル格納用ディレクトリ。主にバックエンド化やドキュメント用)

## 2. 今後の開発プラン (Future Development Plan)

今後の開発は、以下のフェーズに分けて進行することを想定しています。

### フェーズ 1: データ処理の堅牢化と基本機能の拡充
1. **高度なCSVパースの実装**:
   - 現在の簡易的なパース処理を改善するか、`PapaParse`などの堅牢なライブラリを導入し、データ内のカンマや改行、ダブルクォーテーションを正しく処理できるようにする。
2. **サマリーカードの動的計算**:
   - 読み込んだCSVデータに基づいて、「総登録数」「進学」「就職」などの数値を計算し、画面上部のサマリーカード（`#total-count`, `#university-count`, `#job-count`）を動的に更新するロジックを実装する。
3. **年度・属性フィルターの実装**:
   - CSVデータから「年度」などの特定のカラムを抽出し、ドロップダウンフィルター（`#year-filter`）の選択肢を動的に生成する。
   - 選択された条件に基づいてテーブルの表示を絞り込む機能を実装する。

### フェーズ 2: データの可視化とUI/UXの向上
1. **グラフ・チャートの導入**:
   - Chart.jsなどのグラフライブラリを導入し、進路先の内訳（進学vs就職、大学群別の割合など）を円グラフや棒グラフで視覚的にわかりやすく表示する。
2. **データテーブルの機能強化**:
   - カラムヘッダーをクリックしてのソート機能（昇順・降順）。
   - データ量が多い場合に備えたページネーションの実装。
3. **レスポンシブデザインの最適化**:
   - スマートフォンやタブレットでの閲覧時における、テーブルやグラフの表示最適化。

### フェーズ 3: データの永続化とシステム連携（今後の展望）
1. **サーバー連携 / バックエンド化**:
   - 現在の「都度ファイルをアップロードする」方式から、サーバー（Node.js, Pythonなど）を構築し、`input/`ディレクトリなどに配置されたCSVファイルを自動で読み込んでAPI経由でデータを取得する方式への移行。
2. **データベースの導入**:
   - 将来的なデータ増加や複雑な検索要件に備え、CSV運用からSQLiteやPostgreSQLなどのデータベース管理への移行。
